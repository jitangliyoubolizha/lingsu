# content

学习内容数据源，与程序代码完全分开。

规则：
- 每本书一个子目录，例如 `shanghanlun/`、未来的 `jinkui/`；
- 书内按“版本 → 篇 → 条文”组织；
- 方剂、药物、注解、题目也放在对应书的目录下；
- 所有内容文件在构建时会被自动校验，格式和字段以 `docs/数据规范.md` 为准；
- 当前 `shanghanlun/` 包含垂直切片样例，正式内容需人工校对后扩充。
