# Changelog

本文件记录《灵素》每个版本的重要变更。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

## [Unreleased]

### Added

- **内容补全批次——方剂病机 112 首齐备、类方关系补边、症状标签补标、复核题扩充**：①方证对比页「病机」列此前 92/112 首显示「待补充」（症状术语表中「病机」类词条仅 21 个、条文打标覆盖不到），现逐首手工撰写 `pathomechanism` 病机一句话（如桂枝汤「风寒袭表，营卫不和，卫强营弱」、小柴胡汤「邪居少阳半表半里，枢机不利，正邪分争」），112/112 全部齐备，方剂详情页与对比页病机列清零；②类方/加减关系自 55 条补至 113 条——为 49 首空缺方剂补边（小柴胡汤挂大柴胡/柴胡桂枝/柴胡桂枝干姜/柴胡加芒硝/柴胡加龙牡五条，泻心汤四首互勘，白虎加人参汤归白虎汤，承气/陷胸/苓桂剂/治黄三方/少阴咽痛五方等成族），文蛤散、瓜蒂散、炙甘草汤、烧裈散、牡蛎泽泻散、吴茱萸汤、乌梅丸、厚姜半甘参 8 方确无类方关联、如实留空；③症状标签补标：新增 9 个术语（胃家实/蓄血/脾约/脉大/脉沉/目中不了了/睛不和/喜忘/大便难），为阳明提纲 180、脾约 179、急下 252、蓄血 237、脉大 186、急温 323、吐血 019、抵当 124/125、脉迟浮弱 098 共 10 条补标——此前 22 条无标签条文中 14 条属欲解时/治则条合理为空，其余补齐，闲置术语词条由 5 个消化至 1 个（脉阴阳俱微，原文不在 398 条内）；④人工复核题 21 → 36 道：新增 clause_chain 4（小柴胡/桂枝加附子/大陷胸/大承气条接龙）、formula_composition 5（真武/小柴/五苓/炙甘/麻黄）、formula_syndrome_match 3（真武/当归四逆/大承气）、fill_blank 3（热痞/少阳提纲/厥阴提纲），均 reviewed 且按原文出题；测试数量断言同步更新（术语 204→213、题库 21→36），`validate:content` 通过、vitest 319 全绿（2026-08-30）

- **中药板块上线（列表页 + 底部 tab + 本草卡详情 + 方药联动）**：新增 `/herbs` 中药列表页（`HerbListView.vue`，仿方剂列表模式——按功效分类分组带计数、搜索按药名/别名/分类/功效过滤、行内性味速览、空态与页脚免责声明），数据来自主包元数据 `loadMeta().herbs` 零新增请求；底部导航增第 5 个「中药」tab（`AppNav.vue` 叶子图标，桌面侧栏同款，`App.vue` TAB_ORDER 增 `herbs` 支持方向性切页动画）；药物详情页升级为本草卡——性味/归经/功效/用量参考（标注仅供参考）要点行、使用注意朱砂警示块、经方应用段落、收藏星标（Dexie `type:'herb'` 此前已有支持，补上入口）、返回目标改 `/herbs`、页脚固定免责声明（口径同方剂详情）；方剂详情页组成药名全部改为可点击链接直达药物详情（剂量文字留在链接外）；全文搜索药物分组索引文本增补功效分类；数据链路——`types.ts` Herb 增 7 个可选本草字段、`content-builder.ts` toHerb 透传新字段、校验器新增 herbs 规则（category 枚举 22 类含「经方特殊用品」、五文本字段非空、归经非空字符串数组，随本草卡齐备升级为必填）、搜索药物索引与 `content-validation` fixture 同步更新；测试——路由断言、81 味本草卡字段齐备断言、列表/详情组件测试 4 例、e2e 新增链路 4（中药 tab → 列表 → 详情 → 方剂组成联动，`:visible` 定位兼容移动/桌面两种导航），测试 313 → 318、e2e 6 → 7；`validate:content`/`build:content`/`typecheck`/`test`/`build` 全绿，浏览器实测移动/桌面 × 明暗四态新页面视觉验收通过（2026-08-30）

- **名家注解第三批落地（全书覆盖）**：其余 344 条按分批计划「保持项目自编 + 补一家公版注」——自成无己《注解伤寒论》（四库转录本逐条【】嵌注）程序化提取注语 344 条：条文/注语异体字归一 + 去标点对齐，三级匹配（整段→最长前缀→全局）+ LIS 锚定修复顺序错配，自动排除方剂组成剂量块，30 余条疑难（成氏合并条文一注覆盖多条、成氏本次序与宋本差异、转录讹字致匹配失败等）逐条人工对位；现代标点 AI 批量句读并逐字校验（去标点后与转录原文逐字符一致），异体/讹字统一常用字（蘗→柏、讝→谵、縁→缘、痓→痉 等），四库本阙字以 □ 占位、通假原字保留（「阳王于昼」之王）；成注置于条首、自编注保留，`notes` 标「引文待核对原书」；**至此全书 398 条每条均有公版注解**（成无己 386 条 + 柯琴/尤怡/徐大椿/钱潢/张璐/黄元御/张志聪/丹波元简等见第一、二批），全书注解共 848 条；`validate:content` 通过、测试全绿（2026-08-30）

- **中药本草卡第三批落地（活血/平肝/安神/补虚/收涩/涌吐/特殊用品 29 味）——81 味本草卡全部补齐**：本批覆盖活血化瘀药 3（桃仁/水蛭/虻虫）、平肝息风药 2（牡蛎/代赭）、安神药 1（龙骨）、补虚药 9（芍药/甘草/大枣/人参/白术/胶饴/阿胶/麦门冬/当归）、收涩药 4（五味子/乌梅/赤石脂/禹余粮）、涌吐药 2（瓜蒂/蜀漆）、拔毒化腐生肌药 1（铅丹）、经方特殊用品 7（粳米/猪胆汁/人尿/裩裆灰/猪肤/鸡子黄/生梓白皮）；原书特殊物品（食物、动物/人体制剂、梓白皮等无通行本草分类者）归入「经方特殊用品」新类别，按文献通识编写并在 `notes` 说明考辨；至本批 81 味七字段齐备、占位「AI 初稿待校对」清零，`validate:content`/`build:content` 通过（2026-08-30）

- **中药本草卡第二批落地（化湿/利水/温里/理气/化痰 23 味）**：继第一批后再补 23 味——化湿药 1（厚朴）、利水渗湿药 7（茯苓/猪苓/泽泻/赤小豆/通草/茵陈蒿/滑石）、温里药 4（附子/干姜/吴茱萸/蜀椒）、理气药 2（枳实/薤白）、化痰止咳平喘药 9（杏仁/半夏/葶苈子/瓜蒌实/桔梗/贝母/旋覆花/海藻/文蛤）；口径同第一批——`applications` 自撰、以经方运用为主线，事实字段按通行本草通识，`notes` 记录考辨存疑点（通草之木通考、文蛤之海蛤五倍子之争、连轺代用）并标「待校对」；累计 52/81 味；`validate:content`/`build:content` 通过（2026-08-30）

- **中药本草卡第一批落地（解表/清热/泻下 29 味）**：按 PRD §4.4.1 与 `docs/数据规范.md` §4.5 新增的本草卡字段，为 81 味药物分三批补齐 `category`（功效分类）/`nature`（性味）/`meridians`（归经）/`effects`（功效）/`applications`（经方应用）/`dosage`（用量参考）/`cautions`（使用注意）七字段；本批覆盖解表药 9 味（桂枝/麻黄/生姜/葛根/柴胡/细辛/葱白/淡豆豉/升麻）、清热药 12 味（石膏/知母/栝楼根/竹叶/栀子/黄芩/黄连/黄柏/生地黄/白头翁/秦皮/连轺）、泻下药 8 味（大黄/芒硝/甘遂/大戟/芫花/巴豆/麻仁/商陆根）；`applications` 一律自撰口径、以经方运用为主线（对应本库方剂数据），事实性字段按通行本草通识编写，不涉现代出版物文句，全部条目标「本草内容初稿待校对」；资料未收载者（如连轺）按通行替代药数据编写并在 `notes` 说明；`validate:content`/`build:content` 通过（2026-08-30）

- **名家注解第二批落地（33 条 97 家）**：按参考资料 README 分批计划推进「方剂主证条与治则名条」——太阳治则与主要方证（13/14/16/31/34/40/61/63/67/76/82/97/102/103/117）、阳明（219/243/247/252/262）、太阴（279）、少阴（301/303/304/309/317/318/319）、厥阴（350/351/373/378）、差后劳复（397），每条 2–3 家，共 97 条（公版引文 64 + 现代观点化用 33）——公版新增引用《伤寒论注》（柯琴）、《伤寒溯源集》（钱潢）、《伤寒悬解》（黄元御）、《伤寒论集注》（张志聪）、《伤寒论辑义》（元简案语）与《伤寒明理论》；现代化用续按六路口径（刘渡舟 2/胡希恕 1/郝万山 3/陈亦人 1/教材 26）；吴茱萸汤三条与真武汤两条形成跨篇对照组；柯琴《伤寒论注》未收麻子仁丸与竹叶石膏汤（无劳复篇），两条由尤怡、钱潢补位；引文探针回验同第一批，`validate:content`/`build:content` 通过，测试 312 全绿；两批累计 54 条 160 家，注解参考资料 README「落地进度」同步更新（2026-08-30）

- **名家注解第一批落地（打样 21 条）**：按 `docs/注解参考资料/README.md` 分批计划，将六经提纲（1/180/263/273/281/326）与核心主证条（12/20/35/38/71/96/106/135/149/154/177/208/236/316/338）共 21 条的占位注解（`项目自编（待校对）`）替换为名家注解，共 63 条（每条 2–3 家）——①**公版逐字引文** 42 条：成无己《注解伤寒论》/《伤寒明理论》、柯琴《伤寒来苏集》/《伤寒附翼》、尤怡《伤寒贯珠集》、徐大椿《伤寒论类方》、张璐《伤寒缵论》七部公版注本，自 `docs/注解参考资料/原文/` 转录本逐字摘录（转录本无句读且含异体讹字，成稿加句读、统一常用字，如 隂→阴/蚘→蛔/胷→胸，每条 `notes` 保留「公版引文待核对原书」标记，核对纸本前不算定稿）；②**现代观点化用** 21 条：按《现代名家观点参考.md》口径以自编语言转述刘渡舟（提纲纲作用/调和营卫/三焦气化/和解枢机）、胡希恕（阳明里阳证）、郝万山（病机概括与鉴别眼目）、陈亦人（厥阴寒热错杂有规可循）、李克绍（泻心痞证互勘）与教材体系（辨证要点口径），`source` 按「参考《书名》」约定、`author` 署灵素内容组；条文详情页注解标题随之适配——`source` 以「参考《」开头者显示「名家注解 · 观点化用」以示非逐字引文，公版条仍署注家名（`ClausePageContent.vue` 新增 `annotationTitle`）；13 个章节文件头状态注释同步更新；全部引文摘录经检索探针回验转录原文（逐条命中方可采用）；`validate:content`/`build:content` 通过，测试 312 全绿（2026-08-30）

- **名家注解参考资料库**：为「名家注解」内容完善铺路——新增 `docs/注解参考资料/`：①收录 15 部公版注家全文（成无己《伤寒论注释》四库本/《明理论》、柯琴《来苏集》及三分册、尤怡《贯珠集》、徐大椿《类方》、丹波元简《辑义》、陈修园《串解》、黄元御《悬解》、方有执《条辨》、钱潢《溯源集》、张志聪《集注》、张璐《缵论》，约 5.3MB，来源殆知阁 `daizhigev20`，简体转录、引用前须核对纸本）；②`README.md` 写明版权红线（现代注家不可逐字、公版可引、点校本不动）、三条写作口径（公版逐字引标「待核对原书」/现代观点化用标「参考《书名》」/每条 1–3 家组合）与三批落地计划；③《现代名家观点参考.md》自编综述刘渡舟/胡希恕/陈亦人/李克绍/郝万山/教材体系六路口径供化用（不含引文）；④未收录书目与来源备查（ctext 需注册 API、维基文库本机不可达、经方实验录镜像）。落地时 398 条注解为「项目自编（待校对）」占位，内容替换按 README 分批进行（2026-08-30）

- **条文详情横滑翻条（E-15，产品方需求）**：条文详情页支持**左右滑动手势翻条**——按住页面横向拖动内容实时跟手，松手超过 25% 屏宽或快速一甩即翻到下/上一条，不足则 motion 弹簧（210/22）带速度回弹，尽头方向（第一条/最后一条）rubber-band 阻尼不再跟进；**拖动反馈**：整页 3D 微倾（perspective + rotateY ≤4°）随进度轻微缩小，纸页被推动的实感；整页交接完成 `navigator.vibrate(8)` 微震动（不支持的平台静默）；**真分页轨道**（产品方两轮反馈迭代：先「动效生硬」重做转场，再「只见到预览卡好奇怪，为什么不是整页」升级为整页轨道）——详情页为 **[上一页｜当前页｜下一页] 三页轨道**，当前页与相邻页共用同一内容组件（抽出 `ClausePageContent.vue`，含笔记自管理），拖动整条轨道跟手、相邻整页自边缘跟入（副本 `inert + aria-hidden`、顶栏静态化、不加载笔记），松手过阈值整页滑动就位（motion 260ms `ease-out-soft`）后瞬时换页（`page-instant` 转场无第二次过渡，副本与真实页像素一致无缝衔接）；三槽位恒渲染（无数据时空占位，邻居异步加载前后轨道几何恒定，修复进入条文瞬间/动画冻结时相邻页边缘露出遮挡当前页的版面缺陷）；轨道 **页间 24px 间距**（`gap-6`，位移基准 `calc(-100% - 24px)`）——首版零间距使相邻页头 20px 恰好落在内容区两侧的边距带内被看到（产品方截图反馈「静止时下一页和上一页部分内容侵占当前页面」），加间距后移动端静止时相邻页整体位于屏幕外，拖动中页与页有自然书页间隙；内容柱根元素 `overflow-x-clip` 修复桌面宽屏（内容柱 672px 居中、两侧大片留白）相邻页整页外露的版面混乱——相邻页任何断点不越过内容柱，翻页在内容柱窗口内进行（clip 不创建滚动容器，顶栏吸顶不受影响）；动画落定 700ms 超时兜底（无渲染帧环境 `finished` 永不 resolve 时直接就位换页，滑动不卡死）；`pagerSettling` 抑制 scrollBehavior 回顶，滑动翻页落点保持拖动时的阅读位置（链接/键盘翻条仍回顶）；条文间翻页跳过区块编舞与印章动画（整页一体就位），仅从列表/外链进入时播放入场编舞；翻条方向只在导航起点写入、转场期间不变（由「从非条文页进入详情」守卫清除残留，禁止转场结束后翻动 Transition 的 mode/name）；仅触摸/笔启用（鼠标端与文本选择冲突，桌面用键盘 **←/→** 翻条，焦点在笔记输入框时保持原生行为），滑动/键盘/底部链接三种方式共用同一导航入口并统一转场方向；**横滑性能与手感调优**（产品方反馈滑动闪烁）——逐帧只写 `translate3d` 禁用逐帧 3D 倾斜/缩放（巨型图层逐帧重光栅化为闪烁卡顿主因）、页宽按下缓存/监听 passive/回弹改 WAAPI 合成器、阈值调轻（意图 4px、翻页 13% 页宽或 0.24px/ms 轻甩即翻）、横滑后 click 拦截 250ms 防误触；条文同步缓存使整页翻页重挂载首帧即完整渲染消除闪烁；**翻条为 replace 原地换页**（产品方反馈「连续看好多条，返回键却在返回上一条」）——连续翻条不堆历史记录；**返回键不依赖历史栈**（产品方桌面端复测仍逐条返回：旧会话残留的历史无法从 JS 清除）——顶栏返回走 `goBackToEntry`：来源页非条文时 `history.back`（还原来源页滚动位置），上一条历史仍是条文（旧栈污染）时直接回条文库，任何旧会话一步返回；**返回/进入均有方向性转场**（进入条文自右滑入、返回自左滑回，产品方反馈返回动效不明显）；条文同步缓存扩展至相邻页与收藏状态（产品方反馈手机端滑动仍有闪烁：换页重挂载首帧相邻条文/星标状态未就绪）；译文内「第 N 条」互链保留 push 可回跳；`scrollBehavior` 新增条文详情回顶，顺带修复条文间切换后停留在页底的问题；同步设计收尾——原文卡升级**古籍双线朱砂界框**（`clause-frame`，token 适配暗色）、条文编号改**朱砂小方印**（复用 seal-stamp 盖印动画，decorative aria-hidden，`float` 环绕排版不遮正文）、节标题统一前置小朱点（`section-title`）、主要区块错峰浮入（`choreo`，40ms 步进）；新增 `useSwipeNavigate` composable（手势约定同 CardStack §4.2：touch-pan-y、交互元素内不启动、Pointer Events + setPointerCapture）；手势单测 9 例 + e2e 3 例（CDP 真实触摸事件覆盖左滑/右滑/拖动中下一整页跟入/纵向滚动不冲突/尽头边界/键盘/回顶），测试 300 → 309（2026-08-30）

### Changed

- **记忆保持率趋势模块完善（产品方问询「有没有具体功能」后补齐短板，口径两轮反馈定为当周每日）**：趋势计算从 StatsView 组件内下沉为 `domain/stats.ts` 纯函数 `computeRetentionTrend` 并补 4 例单测（此前组件内联逻辑零测试）；口径由「最近 7 周」改为**当周每日**——横轴周一至周日（周一起算，与学习日历一致，第一版「周7…周1」被误读成星期几且无「周7」说法）、每天展示「记得 ÷ 复习次数」；三态区分：当日无复习为「—」+ 虚线空柱、复习过但全忘为 0% 红色短柱（红=忘了，符合色彩语义 §5）、正常为朱砂柱 + 柱顶百分比；本周剩余未到的天整列置灰；标题改「本周记忆保持率」、底部图例说明口径；数据链路本身为真：背诵页复习卡自评（忘了/模糊/记得）→ `saveReviewLog` 入库 → 统计页聚合；新增纯函数测试 4 例，测试 309 → 313（2026-08-30）

- **主 tab 切换反馈增强（E-15，产品方反馈「四个模块切换反馈不明显」）**：底部导航四个主 tab（首页/刷题/统计/我的）之间切换改为**按 tab 左右顺序方向性滑动**——切向右侧 tab 新页自右滑入、切回左侧自左滑入（复用条文翻条的方向性并行转场 `page-next`/`page-prev`），不再走通用淡入淡出；激活项**图标 spring 弹跳**（`nav-icon-pop`：scale 0.7→1.18→1，320ms `ease-spring`，仅激活瞬间播放）+ 顶部**朱砂短横指示条**渐现（`nav-indicator`，250ms），激活/未激活对比更明确；按住 tab 整项轻微缩小（`nav-press`，120ms，同 `pressable` 语言）；桌面侧边导航同款按压与图标弹跳。非 tab 路由进出不启用方向滑动；中间态与落定态截图过视觉验收（方向感可读、指示条位置正确、无布局破坏）；`main.ts` 新增**帧冻结环境兜底**——启动 120ms 探测 `requestAnimationFrame`，冻结环境（内嵌预览窗格等，表现为点返回/切 tab 画面无变化）降级为 setTimeout 驱动，正常浏览器不生效（2026-08-30）


- **首页顶栏主题快捷切换 + 状态栏取色同步（产品方 UX 反馈）**：首页顶栏原「通知」占位死按钮改为日/月主题切换按钮（图标带旋转缩放过渡），点击即在浅色/深色间切换；`theme-color` meta 由「媒体查询跟随系统」改为按**应用内主题**同步——`theme-boot.js` 首帧前与 `useTheme.applyTheme` 运行时双写（应用内深色≠系统深色，iOS 状态栏/主屏幕区域取色此前不跟随应用内切换）；首页移除与统计页重复的「学习统计」区块（本周打卡格子 + 篇章进度条）；方剂列表页顶栏新增「对比」入口，方证对比页返回目标改为方剂列表；我的页移除指向自身的「内容来源」死链；验证全绿，测试数量不变（281）（2026-08-29）

### Fixed

- **首页卡片堆拖拽手感重调——「随手一拨即换」（产品方反馈「有点难拖动」）**：浏览器实测复现并量化——送底阈值写死 290px，而首页卡宽 672px，等于**要拖掉 43% 卡宽才换一张**；且只有距离判据、**没有速度判据**，随手快甩 60/90/120px 全部无反应（拖完弹回原位，"拖了但没用"）。跟手本身是准的（逐帧 transform 与指针位移 1:1，无阻尼）。按本站已调好的条文横滑同一套标准重校：①**送底阈值改按卡宽比例** `clamp(卡宽 × 18%, 48px, 200px)`（替换 `sensitivity` 固定像素 prop，新增 `commitRatio`/`minCommitPx`/`maxCommitPx`）——桌面 672px 卡 290 → 121px、移动端 350px 卡 140 → 63px；②**新增轻甩判据** `flickVelocity`：位移 >24px 且速度 >0.35px/ms 即送底（原「轻甩」手势等于白拖），速度取一阶低通平滑值、改用 `performance.now()` 计时以便单测注入；③**3D 倾斜随送底进度线性映射**——原为固定 ±100px → ∓60°，宽卡上拖过 100px 倾斜就饱和、剩余路程毫无反馈，改为拖到阈值处正好 60°（窄卡上倾斜也更跟手）；④拖动期间给外层加 `will-change: transform`（逐帧 3D 倾斜会重新光栅化），卡宽按下时缓存、拖动中不读布局。验证：浏览器实测阈值由 290px 降至 ~121px、60px 快甩即可换牌、零位移点按仍正常轮换（不被误判为拖拽）；新增单测 5 例（比例阈值上下界、轻甩即换、慢拖不换牌、卡宽不可读退化下限、倾斜线性映射），把手势事件工具抽为 `tests/helpers/pointer.ts`（jsdom 缺 `PointerEvent` 与指针捕获）并让横滑用例复用、顺带修正其过期注释（阈值 25%→13%、甩动 0.5→0.24），测试 319 → 324 全绿（2026-08-31）

- **条文横滑交互手感重调——「小说翻页」式轻拂即翻（产品方反馈「组件上不灵敏、幅度要很大」）**：产品方实测的判断属实——旧手势在 `a/button` 等交互元素上直接不启动，而条文页的译文/注解手风琴标题是**整行 button**、正文还有条文互链链接，页面大部分区域起不了滑；翻页阈值又高达页宽 25%（手机约百像素）+ 甩动速度 0.5px/ms，很难「一翻就过」。本次三处重调：①**任意位置可起滑**——手势排除范围收窄为表单控件（textarea/input/select/contenteditable），按钮/链接上也能起滑翻页；意图成立时才 setPointerCapture 并布防一次性 click 拦截（下一 pointerdown 或首个 click 即解除、250ms 兜底），起手组件上的横滑不会误触点按，不起滑的正常点按完全不受影响；②**轻拂即翻**——翻页距离阈值 25% → 13%（手机约 45px）、甩动速度 0.5 → 0.24px/ms、起手意图 6px → 4px；③**更快的合成器动画**——回弹从 JS 弹簧（逐帧跑主线程）改为 0.2s ease-out WAAPI（合成器执行），交接时长 0.26 → 0.22s；测试——单测改写「按钮上可起滑/表单控件不可」并新增误触 click 拦截用例（测试基建修正：jsdom 原生 PointerEvent 默认不冒泡，fire 助手补 `bubbles: true` 才能覆盖「从子元素起滑」路径），319 单测 + 7 e2e 全绿；真机手感请以局域网预览或部署后验证（线上站点需推送后才有本批改动）（2026-08-30）

- **条文横滑翻条流畅度修复（E-15，产品方反馈「滑动还是不流畅」）**：两处根因——①**拖动跟手阶段逐帧 3D 重绘**：`onPointerMove` 每帧向整条三页轨道写入 `translate3d + perspective + rotateY + scale` 复合变换，逐帧变化的 3D 矩阵迫使浏览器对这块高逾数屏的巨型图层每帧重新光栅化（纯位移只需合成不需重绘），为横滑卡顿主因，现拖动中改为**纯 `translate3d` 位移**并删除微倾/缩放效果；同时页宽在 pointerdown 一次缓存（此前每次 move 读 `clientWidth` 造成逐帧强制样式计算）、指针监听标 `passive`、水平意图阈值 8px 收紧为 6px（起手更跟手）；②**松手换页落点顿挫**：换页 watcher 先清空相邻页副本再异步重建——同步卸载两整页 DOM 再挂载，恰发生在交接动画结束的落点瞬间；现**保留旧副本驻留屏外槽位直到新数据就位**（篇章走内存缓存，一个微任务到达，槽位几何恒定无空档）；单测断言改为锁定「纯位移、无 rotateY/perspective/scale」合成器友好约定，318 单测 + 7 e2e 全绿；预览页合成拖动逐帧采样验证：118 帧 p50/p90 均 6ms、最大 25ms、>32ms 长帧 0 个，且拖动正常完成翻条与复位（2026-08-30）

### Added（历史：2026-08-29）

- **首页经典条文卡片堆**（React Bits「Stack」组件移植，产品方需求）：首页「每日一句」静态引文卡升级为可交互的经典条文卡片堆——15 条名条文候选池（六经提纲 1/180/263/273/281/326 + 桂枝汤、麻黄汤、小柴胡汤、泻心汤、炙甘草汤、真武汤等主证与「随证治之」治则条）按日期**确定性抽取 5 条**展示（新增 `domain/daily.ts` 纯函数：FNV-1a 种子 + mulberry32 洗牌，同一天全站一致、跨天自动轮换，日期口径与今日任务 todayKey 一致），衬线卡 + 朱丝栏，支持拖拽顶牌送底（motion 弹簧带回弹初速度 + 3D 倾斜随位移）、点击/键盘轮换、5s 自动轮换（悬停/切后台/拖拽中暂停）；新增通用组件 `ui/components/CardStack.vue`——原组件为 React + framer-motion，按其 vanilla API 移植为 Vue SFC，动画运行时新增 `motion` 依赖（约 15kB gzip，仓库首个动画库，与「动效打磨」的纯 CSS 路线为经确认的有意取舍）；扇形角度/缩放步进/随机幅度/展开原点参数化——首页取 1.5° 步进、±4° 随机（新增 `random-amplitude` prop）、原点改底部居中 50% 92% 并预留上下呼吸间距（mt-6/mb-12），层叠重排缓动 450ms 改 `ease-out-soft`（大面幅重排不用过冲曲线），**修复宽卡扇形下缘/上缘溢出遮挡「今日任务」卡与搜索栏的问题**（桌面端尤为明显；实测全轮换周期桌面最小净距 ≥10px、移动端 ≥32px、无横向溢出）；触摸端卡片 `touch-action: pan-y`——横向滑动拖拽换牌、纵向滑动照常滚动页面，送底阈值随卡宽自适应（40% 卡宽）；`prefers-reduced-motion` 下停用自动轮换、拖拽即时归位；内容加载失败回退静态引文卡；设计规范 §5 首页条目与动效规范 §4 同步修订；组件测试 6 例 + 抽样纯函数 5 例，测试 289 → 300（2026-08-29）

- **动效打磨**（E-14，产品方 UX 反馈）：新增缓动令牌 `--ease-out-soft` / `--ease-spring`（`@theme` 定义，Tailwind v4 生成 ease 工具类）；页面切换由「250ms 横滑 + 整页旋转」改为「100ms 纯淡出让位 + 220ms 自下轻浮」，消除导航二次等待感；刷题/错题巩固选项接入 `TransitionGroup` 编舞（逐项错峰 40ms、8 项封顶，key 携带题序切题重触发）；答题反馈升级——答对图标轻弹（spring）、答错选中项摇头；设置弹层补齐入场过渡（遮罩淡入 + 面板自下滑入，此前瞬时硬切）；选项行与首页快捷卡统一 `pressable` 按压反馈（120ms 弹性缩放）；翻转卡缓动换 `ease-out-soft`；`prefers-reduced-motion` 升级为全局兜底；不引第三方动画库，纯 CSS 令牌 + Vue 内置 Transition；新增动效令牌测试 8 例，测试 281 → 289（2026-08-29）

- **「设置」按钮激活 + 功能收纳（产品方 UX 反馈）**：我的页右上角「设置」按钮此前是无事件的占位死按钮，现点击弹出底部弹层，收纳**每日新学数量、字号大小、朗读语音**三项（数据同源、即时生效）；页面主体学习设置区块相应精简为「外观」（仅保留外观主题），页面更短；测试 279 → 281（2026-08-26）

- **今日任务页就地调整任务量（产品方 UX 反馈）**：把「去设置跳转」改为今日任务页顶部**常驻任务量控件**——预设 3/5/8/10 chips + 自定义 1~20 输入，点选即写设置并联动进行中计划（新增 store pplyDailyNew 一步入口），今日队列立即按新任务量重算；首次引导卡精简为一句说明 +「我知道了」（仍记录 dailyTipDismissed），控件不随引导关闭消失；测试 276 → 279（2026-08-26）

- **今日任务量上调与自定义（产品方需求）**：默认背诵任务量 3 → 5 条（新建默认计划与设置默认值同步上调）；今日任务页首次进入显示任务量提醒卡（推荐 5~8 条，含「去设置」与「我知道了」，关闭后记录 `dailyTipDismissed` 不再打扰）；「学习设置 → 每日新学数量」在 3/5/10 基础上新增 **8 条**与**自定义 1~20 条**（越界自动钳制），并修复该设置此前**不作用于今日任务队列**的缺陷——现在修改会同步更新全部进行中计划（`updateActivePlansDailyNew`），今日任务立即生效；新增计划表单同样支持自定义条数；store 真库 3 例 + 今日任务提醒 3 例 + 我的页 5 例，测试 264 → 276（2026-08-26）

- **条文笔记/批注**（E-5）：条文详情页新增「我的笔记」卡——textarea 输入理解/疑问/记忆要点，保存至 IndexedDB `notes` 表（clauseId 主键，一条条文一篇笔记，清空保存即删除）；数据库升 v3，备份格式升 schemaVersion 2（notes 表纳入导出/导入，v1 旧备份缺表按空表兼容导入）；笔记随备份跨设备迁移；store 真库测试 5 例 + 备份兼容 1 例 + 组件测试 3 例（保存/回填/删除），测试 255 → 264（2026-08-26）

- **图谱症状维度 + 学习闭环**（E-12）：图谱新增症状节点层（默认关闭，可开关；`s:` 前缀节点），三类新边——present 条文见症（随条文开关）、suggests 方剂提示症（相关条文中 ≥2 次共现推导，同药对思路）、targets 方剂主症（mainSymptoms 显式录入，数据待录）；搜索未命中时自动在症状层探测并临时开启；引擎症状边以赭金虚线绘制区分结构关系；信息面板新增「刷相关题 →」闭环入口（方剂/条文节点），刷题页支持 `?formula=` / `?clause=` 深链筛选；数据构建 5 例 + 过滤器 4 例 + 图谱视图 2 例 + 刷题深链 2 例，测试 242 → 255（2026-08-26）

- **深色模式（宣纸暗夜）**（E-1）：`style.css` 新增 `html.dark` 全量令牌覆盖，Tailwind v4 工具类引用 CSS 变量实现全站自动切换；我的页「学习设置 → 外观主题」三档切换（浅色/深色/跟随系统，默认跟随系统），存 localStorage 并由 `public/theme-boot.js` 首帧前打类防白闪（CSP 自源脚本合规）；新增对比度门禁测试——从 style.css 解析两主题令牌、正文/强调色全部 ≥ WCAG AA 4.5:1（浅色 ink-muted 因门禁由 `#8A8272` 加深至 `#6F675A`）；纯函数测试 5 例 + 门禁 3 例，测试 234 → 242（2026-08-26）

- **合规提示智能收纳 + 协议同意持久化双保险**：底部合规提示条新增 ✕ 收起按钮，收起后 7 天静默（`useNoticeBar` + settings 表 `noticeDismissedAt`），期满自动恢复，条文详情/搜索页自带横幅不受影响；协议同意状态由仅存 IndexedDB 改为 IDB + localStorage 双写双读、任一存储被清或写失败不再导致整页协议反复弹出（移动端浏览器定期清理站点存储的常见痛点）；设计规范 §6 合规红线同步修订；settings 测试 4 例 + 收纳策略 4 例，测试 226 → 234（2026-08-26）

- **知识图谱模块**（T3-2 / E-7，原计划 echarts 改为零依赖自研引擎）：新增 `domain/graph/data.ts` 纯函数构建方剂/中药/条文三类节点与组成/出处/药对三类边（药对 = ≥2 方剂共现推导）、邻居子图抽取、图内搜索定位（支持中药别名与条文号）；`ui/graph/ForceGraph.ts` Canvas 力导向引擎（拖拽/平移/滚轮+双指缩放/悬停高亮），落实 dispose 生命周期、布局保位、dpr≤2、alphaDecay 收敛调优；新增 `/graph` 页：条文节点开关（默认关）、搜索定位（纯数字自动临时开启条文）、只看邻居（含焦点跟随）、导出 PNG、信息面板跳详情页、`?focus=&texts=1` 深链；方剂/中药/条文详情页新增「在图谱中查看」入口、首页新增图谱卡片；数据构建 16 例 + 引擎 7 例 + 邻居模式组件回归 3 例，测试 200 → 223（2026-08-26）

### Changed

- **typecheck 与 CI 构建同口径**：`typecheck` 脚本由 `vue-tsc --noEmit`（在纯引用型根配置下实际未检查文件）升级为 `vue-tsc -b`，与 `build` 同一闸门；此前图谱测试桩的 canvas getContext mock 类型不匹配导致本地全绿而 CI 构建两连败、Pages 停更，修复桩写法（defineProperty）并对齐脚本（2026-08-26）

- **仓库隐私清理**：`docs/推送指南.md` 含本机路径/代理端口等环境信息，移出版本库并加入 .gitignore（本地保留）；全库扫描确认无凭据类内容（2026-08-26）

- **合规提示条收纳策略**：`design/设计规范.md` §6 全局底部提示规则由「不可关闭、不可折叠」修订为「默认常驻，可一键收起并静默 7 天；条文详情/搜索页自带横幅始终展示」（2026-08-26）

### Added（2026-08-25 及更早）

- **发布测试版**（T1-7）：发布检查单全绿——新增免责声明全文页 `/disclaimer`（与 `docs/用户协议与免责声明.md` v0.2 逐节对应，我的页「免责声明」入口由摘要页改为全文页，协议页底部补「查看完整协议」链接）；5 个散剂/特殊制剂方剂（烧裩散/猪肤汤/苦酒汤/半夏散/四逆散）补剂量「待专业校对」占位；打 v0.1.0 tag，新增 `docs/发布与回滚.md`（发布流程/回滚步骤/24h 抽查清单），测试 198 → 200（2026-08-25）

- **主包瘦身**（方剂数据按需加载）：`meta.json` 中完整方剂数据（71.8KB，含组成/煎服/剂量/类方关系）拆为独立 `formulas.json` 按需异步 chunk，`meta.formulas` 仅保留 `id/name/category` 摘要（6.1KB）；新增 `loadAllFormulas`/`loadFormula` 懒加载 API 与 `FormulaSummary` 类型；方剂详情/药物详情/对比/搜索/刷题按需加载完整数据，方剂列表/收藏/条文详情仅用摘要；主 chunk 511.31KB → 412.12KB（>500kB 警告消除），测试 197 → 198（2026-08-25）

- **条文间互链**（T1-5 / E-3）：新增 `domain/clauseLink.ts` 纯函数（中文数字转阿拉伯、解析「第 N 条」、文本分段）与 `ClauseLinkText` 组件，接入条文详情页原文/译文/名家注解；原文中「第 N 条」（阿拉伯或中文数字）自动渲染为可点击链接跳转对应条文，超出全书范围保持纯文本；纯函数测试 12 例 + 组件测试 5 例，测试 180 → 197（2026-08-24）

- **收藏独立页**（T1-4）：新增 `/favorites` 收藏页，按条文/方剂/药物三组展示收藏内容，支持跳转详情与直接取消收藏；我的页「我的收藏」入口由条文列表改为收藏页；新增 `FavoritesView` 与组件测试 3 例，测试 177 → 180（2026-08-24）

- **学习计划管理**（T1-3）：我的页新增学习计划管理——列出全部计划（进行中/已暂停/已完成标签）、暂停/激活切换、删除、新增计划（选择篇章 + 每日新学 3/5/10）；强制最多 2 个 active 计划（`createStudyPlan` 超限返回 null、`togglePlanStatus` 激活前检查上限，常量 `MAX_ACTIVE_PLANS`）；新增 `src/store/studyPlans.spec.ts` 真库测试 13 例 + `tests/profile-study-plans.spec.ts` UI 组件测试 5 例，测试 149 → 177（2026-08-24）

- **安全头配置**（TD-7）：`index.html` 注入 CSP（Content-Security-Policy）与 Referrer-Policy meta 标签，限制脚本/样式/字体/图片/连接来源均为同源；GitHub Pages 自带 HSTS/X-Frame-Options/X-Content-Type-Options；159 测试 + lint/typecheck/validate:content/build 全绿（2026-08-23）

- **测试覆盖率门禁**（TD-4）：安装 `@vitest/coverage-v8@3.2.7`；`vite.config.ts` 配置 domain ≥90% / store ≥80% 阈值；新增 `npm run test:coverage` 脚本；CI workflow 接入覆盖率步骤；纯 Dexie 代理层（cards/favorites/studyPlans/settings）排除（由 store-db 集成测试保证覆盖）；28 文件 159 测试全绿，覆盖率门禁通过（2026-08-23）

- **E2E 冒烟测试**（TD-5）：安装 `@playwright/test`；新增 `playwright.config.ts` + `e2e/smoke.spec.ts` 三条关键链路——协议→首页→背诵 / 搜索→条文详情 / 刷题→错题本；新增 `npm run test:e2e` 脚本；CI workflow 接入 `playwright install --with-deps` + `test:e2e`；本机跑通 3 passed（2026-08-23）

- **完成伤寒论全篇内容录入**（T1-1）：新增 7 篇 220 条条文（阳明病 84 条、少阳病 10 条、太阴病 8 条、少阴病 45 条、厥阴病 56 条、霍乱病 10 条、阴阳易差后劳复 7 条），总计 398 条覆盖完整原文；新增方剂、药物、症状术语并启用全部 10 篇；新增 `docs/方剂目录.md`（`npm run build:formulas-doc` 自动生成），便于集中查看与维护全部方剂（2026-01-22）

- 内容按篇懒加载（T1-6/TD-2/E-9）：构建产物拆为 `meta.json`（79KB，随主包）+ `chapters/<code>.json`（按篇异步 chunk，加载器带缓存）；新增 `loadMeta`/`loadChapter`/`loadAllChapters`/`chapterCodeOfClause` API；条文详情页只加载所属篇、方剂/药物/我的页只用元数据；搜索/刷题/每日任务等跨篇页面按全量加载但共享缓存；测试 142 → 149 用例（2026-08-22）

- 刷题筛选模式（T0-2）：进入刷题页先选模式——随机综合（整卷洗牌）、按篇（太阳病上/中/下）、按题型（四种）、待巩固错题，与错题重做并列；支持 `?mode=random` / `?chapter=` / `?type=` 深链直达；新增 domain 纯函数 `filterQuizDeck` / `shuffleDeck`，共 21 条新测试（2026-08-21）

- GitHub Pages 部署：`BASE_PATH` 环境变量支持子路径构建，PWA manifest/scope/start_url 随 base 调整，新增 `.github/workflows/deploy-pages.yml` 自动部署；woff2 字体改为按需运行时缓存，precache 从 222 项（11MB）降到 17 项（0.9MB）（2026-08-20）

- 错题轻 Leitner 排期：答错次日到期、答对 3 天后复测、连续答对 2 次已掌握；到期错题并入每日任务（每轮最多 10 道、计入打卡），错题重做只取今日到期（2026-08-19）
- 错题本改为「待巩固」台账：正面汇总（今日到期/之后到期/已掌握）、分组折叠、去掉红色“答错 N 次”，手动“已掌握”移出（2026-08-19）
- 意见反馈：站内表单 `/feedback`（类型/位置/描述/联系方式）→ mailto 发送至 l2752255876@gmail.com；入口为全局提示条、条文/搜索横幅、我的页，以及条文详情/学习卡片顶栏（自动预填当前条文位置）（2026-08-19）
- 全局底部合规提示条：`GlobalNoticeBar` 低调纸色常驻（协议页除外），条文详情/搜索页保留自带横幅并让位避免叠加（2026-08-19）
- 古籍书卷气视觉：宣纸纤维纹理背景、暖色令牌、朱丝栏 `.zhusi-rule`、页眉朱砂印章、打卡盖章动画 `.seal-stamp`（2026-08-19）
- 测试补齐：错题排期纯函数与迁移、每日任务错题卡、待巩固台账、streak 边界、统计页/方剂页 UI、反馈表单与纠错入口、题库选项顺序，共 121 用例（2026-08-19）
- 错题本闭环：人工复核题优先（`buildQuizDeck`）、错题自动收录、`/quiz?wrong=1` 错题重做、`/wrong-book` 待巩固台账（2026-08-19）
- 护眼纸墨色系调整：宣纸暖米黄背景与暖褐墨色，同步设计规范与 PRD（2026-08-19）

### Changed

- 主 chunk 体积优化：内容按篇拆分后，条文正文不再打进主包（含重复存储消除），主 chunk 从 500kB+（有警告）降到 453kB（警告消除）；precache 从 17 项（0.9MB）变为 20 项（826KB）（2026-08-22）
- 提示策略瘦身：首次协议页一次性提示补充“条文可能存在错误，可点击底部『内容纠错』反馈”（2026-08-19）
- 产品方决定专业人工校对后置：内容以 AI 初稿上线，以用户反馈为持续纠错机制；同步更新 PRD、项目计划、MVP 验收清单、维护与迭代计划、内容录入与校对流程、用户协议与免责声明（2026-08-19）

### Removed

- 移除未使用的 `echarts` 依赖（TD-1）：`npm ls echarts` 为空；统计页图表为自绘实现；若后续做方药图谱（E-7/T3-2）再按需引入（2026-08-21）

### Fixed

- 太阳病下篇条文顺序错乱（148–178、128–147 交错排列）：内容构建按文件名拼接时 `taiyang-xia-2` 排在 `taiyang-xia` 前，导致条文列表与「上一条/下一条」导航错乱；改为章内统一按条文号排序（2026-08-22）
- 选择题正确选项全为 A：题库组装时对每题 Fisher–Yates 洗牌并重算 `answerIndex`，每次进入刷题页随机（2026-08-19）

## [0.1.0] - 待发布

MVP 内测版（发布时补充完整说明）。
