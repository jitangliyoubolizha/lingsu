# 项目计划（子计划拆分）

> 版本：v0.1
> 用途：把项目从“文档规划”拆成可执行的子计划，每个子计划明确前置条件、要实现的结果、完成后的产物。
> 配套：`docs/PRD.md`、`docs/开发规范.md`、`docs/工程初始化方案.md` 等。

---

## 1. 子计划总览

| # | 子计划 | 依赖 | 状态 |
|---|---|---|---|
| 0 | 工程初始化 | 无（文档已齐） | 已完成 |
| 1 | 内容数据层 | 0 | 已完成 |
| 2 | 核心 domain 模块 | 1 | 已完成 |
| 3 | 本地存储与备份 | 2 | 已完成 |
| 4 | UI 框架与页面 | 0 | 可与 1–3 并行 | 已完成 |
| 5 | 核心功能集成 | 2、3、4 | 已完成 |
| 6 | PWA 与离线 | 5、视觉资源方案 | 已完成 |
| 7 | 内容批量录入与校对 | 1、内容录入流程 | 进行中（太阳病上篇、中篇 31–60 已录入） |
| 8 | 测试与验收 | 5、7 | 未开始 |
| 9 | 上线准备 | 8 | 后置 |

---

## 2. 子计划详情

### 子计划 0：工程初始化

- 📌 **前置条件**
  - `docs/工程初始化方案.md` 已确认；
  - 本机已安装 Node.js LTS 和 npm。

- 🎯 **要实现的结果**
  - 创建可运行的 Vite + Vue 3 + TypeScript + Tailwind CSS v4 工程；
  - 配好 ESLint、Prettier、Vitest、路由、PWA 基础配置；
  - 跑通 `dev / lint / typecheck / test / build`。

- 📦 **完成后的产物**
  - `package.json`、`package-lock.json`
  - `vite.config.ts`、`tsconfig*.json`
  - `index.html`
  - `src/main.ts`、`src/App.vue`、`src/style.css`
  - `eslint.config.js`、`.prettierrc.json`
  - `.github/workflows/ci.yml`
  - 基础依赖登记到 `docs/开发规范.md`

---

### 子计划 1：内容数据层

- 📌 **前置条件**
  - 子计划 0 完成；
  - `docs/数据规范.md`、`docs/内容校验脚本规格.md` 已确认；
  - `content/shanghanlun/` 垂直切片样例已存在。

- 🎯 **要实现的结果**
  - 内容 YAML 可被读取、校验、构建为类型化数据；
  - 校验失败时输出具体文件、字段和修正建议；
  - 垂直切片样例通过校验并可加载。

- 📦 **完成后的产物**
  - `scripts/validate-content.ts`
  - `scripts/build-content.ts`
  - `src/data/types.ts`
  - `src/data/generated/*.json`（构建产物）
  - 垂直切片校验通过记录

---

### 子计划 2：核心 domain 模块

- 📌 **前置条件**
  - 子计划 1 完成；
  - `docs/核心模块设计.md`、`docs/学习计划与统计口径.md` 已确认。

- 🎯 **要实现的结果**
  - FSRS 卡片创建、复习、到期查询可用；
  - 每日队列生成可用，支持最多两个 active 计划；
  - 四种题型可自动生成，自动题 ID 稳定；
  - 搜索索引、中文切词、分组高亮可用；
  - 方证对比字段可自动推导。

- 📦 **完成后的产物**
  - `src/domain/memory/*`
  - `src/domain/quiz/*`
  - `src/domain/search/*`
  - `src/domain/formula/*`
  - 对应单元测试（`*.spec.ts`）

---

### 子计划 3：本地存储与备份

- 📌 **前置条件**
  - 子计划 2 完成；
  - `docs/核心模块设计.md` §2、§6 已确认。

- 🎯 **要实现的结果**
  - Dexie 各表可读写；
  - 设置、卡片、日志、错题、收藏可持久化；
  - 数据导出/导入可用，导入前自动备份当前数据；
  - 数据版本迁移可用。

- 📦 **完成后的产物**
  - `src/store/db.ts`
  - `src/store/*.ts`（设置、卡片、日志、错题、收藏等）
  - `src/store/migrations.ts`
  - 备份导入导出功能
  - 迁移/备份测试

---

### 子计划 4：UI 框架与页面

- 📌 **前置条件**
  - 子计划 0 完成；
  - 13 屏设计稿和 `design/设计规范.md`、`design/交互状态与动效规范.md`、`design/响应式与桌面适配规范.md`、`design/无障碍规范.md` 已确认。
  - 口径说明：设计“13 屏”为原型画布屏，与 PRD §8 的“13 个功能页面”不是一一对应；功能页面以 PRD §8 为准，方剂列表页、药物详情页按 `design/关键页面设计补充.md` 实现。

- 🎯 **要实现的结果**
  - 路由和导航（手机底部导航 / 桌面左侧导航）可用；
  - 13 个页面可访问；
  - 页面样式、组件状态、动效、响应式、无障碍符合设计规范。

- 📦 **完成后的产物**
  - `src/ui/views/*`（13 个页面视图）
  - `src/ui/components/*`
  - `src/ui/composables/*`
  - 路由配置
  - 基础组件库

---

### 子计划 5：核心功能集成

- 📌 **前置条件**
  - 子计划 2、3、4 完成。

- 🎯 **要实现的结果**
  - 首次协议流程可用；
  - 每日任务（新学 + 复习 + 打卡）可用；
  - 刷题、错题本、收藏可用；
  - 统计页指标计算正确；
  - 设置项可用。

- 📦 **完成后的产物**
  - 核心功能闭环页面
  - 与 `src/domain`、`src/store` 的集成层
  - 关键流程测试

---

### 子计划 6：PWA 与离线

- 📌 **前置条件**
  - 子计划 5 完成；
  - `design/视觉资源准备方案.md` 已确认。

- 🎯 **要实现的结果**
  - 应用可安装到主屏；
  - 主要页面离线可打开；
  - 字体、图标、定制 SVG 本地化；
  - 离线缓存策略生效。

- 📦 **完成后的产物**
  - `public/icons/*`
  - `public/fonts/*`
  - `src/assets/svg/*`
  - manifest / service worker 配置
  - 离线验收记录

---

### 子计划 7：内容批量录入与校对

- 📌 **前置条件**
  - 子计划 1 完成；
  - `docs/内容录入与校对流程.md` 已确认。

- 🎯 **要实现的结果**
  - 太阳病三篇（或按版本范围）条文、方剂、药物、术语、题目录入；
  - 所有内容通过校验；
  - 译文、注解、版权来源完成校对。

- 📦 **完成后的产物**
  - `content/shanghanlun/sb/**/*.yaml` 正式内容
  - 校对记录
  - 内容校验通过报告

---

### 子计划 8：测试与验收

- 📌 **前置条件**
  - 子计划 5、7 完成；
  - `docs/测试计划.md` 已确认。

- 🎯 **要实现的结果**
  - `lint / typecheck / test / validate:content / build` 全绿；
  - PRD §13 MVP 验收清单逐项通过；
  - 修复验收中发现的问题。

- 📦 **完成后的产物**
  - 完整测试用例
  - MVP 验收报告
  - 问题修复记录

---

### 子计划 9：上线准备（后置）

- 📌 **前置条件**
  - 子计划 8 完成；
  - 部署平台、域名、备案/合规等事项启动。

- 🎯 **要实现的结果**
  - 部署到静态托管平台；
  - 域名解析、HTTPS 可用；
  - 公开运营前完成必要合规准备。

- 📦 **完成后的产物**
  - 线上访问 URL
  - 发布检查单
  - 备案/合规材料（如需）

---

## 3. 执行建议

1. 子计划 0 是唯一必须先做的事；
2. 子计划 1 和 4 可并行；
3. 子计划 7 内容录入可尽早开始，与 2–6 并行；
4. 子计划 9 上线事项按“要用再说”原则后置。
