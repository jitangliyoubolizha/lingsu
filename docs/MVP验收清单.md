# MVP 验收清单

> 版本：v0.1
> 对照：`docs/PRD.md` §13
> 用途：子计划 8 的验收跟踪；随测试覆盖与用户反馈纠错进度持续更新。

## 状态图例

- ✅ 已有自动化测试或校验覆盖通过
- 🟡 部分覆盖（缺 UI 实测或个别子项）
- ⏳ 待办（未实现 / 待真机实测 / 待人工核验）

> 产品方决定（2026-08-19）：**专业人工校对后置**，不再作为 v0.1 发布门槛；内容以 AI 初稿（draft）上线，上线后以**用户反馈作为纠错机制**持续修订。结构完整性与功能可用性仍按自动化验收；医学正确性以醒目免责声明 + 反馈渠道兜底。

| # | PRD §13 验收项 | 状态 | 说明 / 依据 |
|---|---|---|---|
| 1 | 可安装到手机主屏，离线可打开主要页面与已学内容 | 🟡 | PWA 构建产物、manifest、sw、离线缓存已生成；**真机安装与离线实测待做** |
| 2 | 太阳病三篇条文可浏览、搜索、收藏 | 🟡 | 178 条已录入；搜索 token/索引/分组有 domain 测试，SearchBar/HighlightText 有组件测试；收藏 UI 流程未专门覆盖；内容为 AI 初稿，正确性走用户反馈纠错 |
| 3 | 每条条文有原文、白话译文、至少 1 条名家注解 | ✅ | 结构层由 `validate-content` 强制（含新增负向测试），178 条全部通过；正确性不设人工校对前置，以用户反馈纠错 |
| 4 | 方剂页展示组成、原文剂量、古今剂量换算、煎服法、相关条文、类方关系、安全提示 | 🟡 | 75 首方剂数据齐全（含相关公式字段）；方证推导有 domain 测试；方剂页 UI 展示未专门覆盖；剂量出处标注后置（随反馈完善，不阻塞发布） |
| 5 | 每日任务按 FSRS 生成，完成可打卡，连续天数正确 | 🟡 | FSRS/队列有 domain 测试；StudyTaskView 每日任务翻面/自评/完成打卡流程已有组件测试；**连续天数(streak)计算未专门测试** |
| 6 | 四种选择题型可刷，错题自动进错题本并复现 | ✅ | 四种题型生成有测试；人工复核题优先（`buildQuizDeck` 复核题在前、按稳定 ID 去重）与错题本闭环（答错 upsert、错题重做模式、答对 resolve、错题本列表/移出）均有测试覆盖 |
| 7 | 学习统计正确展示 | 🟡 | stats domain 计算有测试；统计页展示未专门覆盖 |
| 8 | 数据可导出/导入 | ✅ | 新增 Dexie 真库测试：导出导入往返、非法备份拒绝、导入前自动备份、写入失败回滚 |
| 9 | 所有页面含免责声明入口，方剂与检索页有固定安全提示 | 🟡 | 协议页同意流程有组件测试；全页面免责声明入口与固定安全提示未逐页验收 |
| 10 | 内容构建校验通过，无缺译文、无方药关系断裂 | ✅ | `npm run validate:content` 通过；新增 11 条内容校验负向测试；但“无缺译文/无断裂”指结构校验，非专业医学正确性 |

## 测试基线（子计划 8）

- 单元/组件/真库测试：**81 用例**（`npm test`）
  - 内容校验负向：11
  - domain 缺口：10
  - store/Dexie 真库：6
  - store/backup 纯函数：5
  - UI 组件：7
  - StudyTaskView 每日任务：2
  - 既有 domain/stats 等：其余
- `npm run validate:content` ✅
- `npm run lint` ✅
- `npm run typecheck` ✅
- `npm run build` ✅（有 chunk>500kB 性能提示，非阻塞）

## 挂起项（不因本轮全绿而关闭）

- [ ] 用户反馈纠错机制上线（反馈入口/渠道 + 修订闭环，见 `docs/维护与迭代计划.md` T0-7）
- [ ] 中医专业人工校对（**后置**：按用户反馈量与错误率再评估，不阻塞 v0.1）
- [ ] 剂量换算出处标注（**后置**：随用户反馈完善，不阻塞发布）
- [ ] 真机 PWA 安装与离线验收
- [ ] streak 连续天数、统计页/方剂页 UI 展示专项验收
- [ ] 全页面免责声明入口与固定安全提示逐页核验

> 除标注“后置”的项外，其余完成后回到本表将 🟡/⏳ 更新为 ✅，之后进入子计划 9 上线准备。
